import React from 'react';

const Header = () => {
    return (
        <header>
            <nav>
                <h2>this is header</h2>
            </nav>
        </header>
    );
};

export default Header;